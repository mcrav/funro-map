export enum LayerIds {
  POINTS = "points",
  POINT_LABELS = "labels",
  POINT_LABELS_ALTITUDE = "labels-altitude",
  CLUSTERS = "clusters",
  CLUSTER_COUNTS = "cluster-counts",
}

export enum SourceIds {
  MOUNTAINS = "mountains",
}
